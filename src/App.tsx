import React, { useState } from "react";
import { Results, Predict } from "./components";

export const App = () => {
    return (
        <>
            <Results />
            <Predict />
        </>
    )

};
